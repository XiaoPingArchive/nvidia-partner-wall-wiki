# 贡献指南 (Contributing Guide)

感谢您有兴趣参与 **NVIDIA 诸神之墙** 交互式 AI 生态黄页与研习系统的维护与更新！

本项目是一个开源的学术研究与供应链数据库。我们非常欢迎您协助补充缺失的公司官网介绍、修正技术定位描述、或是补充新展出的合作伙伴。

---

## 🛠 如何参与数据编辑与维护

本项目的交互网页数据是高度模块化和自动生成的，请按照以下标准流程提交修改：

### 第一步：Fork 本仓库
1. 点击仓库右上角的 **Fork** 按钮，将项目复制到您的个人 GitHub 账号下。
2. 将 Fork 后的仓库克隆到您的本地机器。

### 第二步：修改数据源文件
请**不要直接修改 `app.js`**，因为它是自动编译生成的。
* **修改公司名录与描述**：
  打开 [generate_directory.py](generate_directory.py)，在 `partner_rows` 列表中找到对应行（Row 1 到 Row 13），直接修改或添加对应的元组：
  ```python
  # 格式为: ('展示名称', '官方网站域名', '中文生态定位简述')
  ('AAEON', 'aaeon.com', '研扬科技，全球先进工业电脑与边缘计算平台制造商'),
  ```

* **补充重点公司深度分析**：
  如果您想为核心伙伴提供更长的高质量分析，可以编辑 [generate_all_companies.py](generate_all_companies.py) 中的 `core_data` 字典。

### 第三步：重新编译生成数据
在本地项目根目录下运行编译脚本，这会自动合并您更新的数据并重建前端 `app.js`：
```bash
python3 generate_all_companies.py
```
运行成功后，终端应提示：`Rewrote app.js successfully with all 285 companies and Easter eggs!`。

### 第四步：本地测试与验证
在浏览器中直接双击打开 `index.html`，或者启动本地静态服务器。
* 搜索并点击您修改的公司 Logo，确保：
  1. 弹出框（Detail Modal）内能够正常显示信息。
  2. 沉浸式研习模式（Study Mode）下的侧边卡片与介绍显示完整。
  3. 没有控制台 JS 报错。

### 第五步：提交 Pull Request (PR)
1. 提交您本地的修改并推送（Push）到您的 Fork 仓库。
2. 在 GitHub 上点击 **New Pull Request**，向我们的 `main` 分支提交合并请求。
3. 我们会尽快进行代码与文案审核并合并。

---

## 📝 文案与排版规范

为了保持整个研究系统的学术性与客观度，请遵循以下撰写规范：
1. **去自媒体化**：文字表达需严谨、客观、中立，禁止使用夸张的口播语调或营销口号。
2. **拒绝个人印记**：项目中不允许带入任何特定的个人播主称呼或视频代称。
3. **保持中英对照**：尽量保留企业在 Computex 展示墙上的原始英文/拼音缩写名称作为 `展示名称`。
